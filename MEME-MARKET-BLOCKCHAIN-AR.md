# شرح Meme Market الحقيقي

هاد الملف كيوثق كيفاش خدام إنشاء وشراء وبيع Meme Tokens فـ Rialo PM.

## العقود المستعملة

- `contracts/RialoMarketFactory.sol`
  - العقد الرئيسي اللي كينشئ التوكنات وكيحتافظ بالسيولة.
  - فيه `createToken` لإنشاء توكن جديد.
  - فيه `buyToken` للشراء.
  - فيه `sellToken` للبيع.
  - فيه `getPool` لقراءة السعر والسيولة.
- `RialoMarketToken`
  - عقد ERC-20 صغير داخل نفس الملف.
  - فيه `balanceOf` و`transfer` و`approve` و`transferFrom`.
  - كل Meme جديد كيحصل على عنوان عقد خاص به.

## شنو كيوقع ملي كيتصنع Meme

1. المستخدم كيدخل الاسم والرمز والكمية والسعر والسيولة الأولى.
2. المحفظة كتوقع معاملة حقيقية.
3. `RialoMarketFactory` كينشر عقد ERC-20 جديد.
4. 20% من الكمية كتمشي لمحفظة المنشئ.
5. 80% كتبقى فـ pool باش الناس يشريوها.
6. السيرفر كيسجل العنوان والصورة والوصف باش يعرض التوكن فالموقع.

إلا نجحات المعاملة ولكن وقع مشكل فالاتصال بالسيرفر، الواجهة كتبحث على آخر توكن مطابق خلقاتو نفس المحفظة وكتسجلو فالسوق بلا ما ترسل معاملة إنشاء ثانية.

## الشراء

1. المستخدم كيدخل كمية RLO اللي باغي يصرف.
2. الواجهة كتحسب تقريباً شحال غادي ياخد من التوكن.
3. كيتحدد حد أدنى حسب Slippage.
4. المحفظة كتأكد `buyToken`.
5. العقد كيرسل ERC-20 مباشرة لمحفظة المشتري.
6. الواجهة كتقترح `Add Token to Wallet` باش يبان التوكن داخل المحفظة.

رفض إضافة التوكن للواجهة ديال المحفظة ما كيلغيش الشراء؛ الرصيد كيبقى موجود فعلياً فالعقد ويمكن قراءته بـ `balanceOf`.

## البيع

1. الواجهة كتقرا الرصيد الحقيقي من عقد ERC-20.
2. المستخدم كيختار الكمية اللي باغي يبيع.
3. أول مرة كيدير `approve` للعقد الرئيسي.
4. من بعد كيأكد `sellToken`.
5. التوكنات كترجع للـ pool وRLO كيرجع للمحفظة حسب السعر والسيولة.
6. مايمكنش يبيع أكثر من الرصيد الحقيقي اللي عنده.

## السعر

السعر ماشي ثابت. كيتحسب من احتياطي RLO واحتياطي التوكن باستعمال pool من نوع constant product. الشراء كيرفع السعر والبيع كيهبطو. الرسوم الحالية هي 1%، وSlippage كيحمي المستخدم إلا تبدل السعر قبل تأكيد المعاملة.

## مصدر الحقيقة

- رصيد Meme Token: عقد ERC-20 على البلوكشين.
- رصيد المستخدم: المحفظة والعقد، ماشي قاعدة بيانات الموقع.
- المعاملات: السيرفر ماكيقبلهاش فالسجل حتى يلقى Transaction مؤكدة وفيها حدث `TradeExecuted` الصحيح.
- قاعدة البيانات كتستعمل فقط للعرض السريع: الاسم، الصورة، الوصف، الرسم البياني والتاريخ.

## الملفات المهمة

- `contracts/RialoMarketFactory.sol`: منطق التوكن والشراء والبيع.
- `rialo-market-factory.artifact.json`: ABI وBytecode اللي كتستعملهم الواجهة.
- `script.js`: ربط المحفظة، إنشاء التوكن، الشراء، البيع، قراءة الرصيد وإضافة التوكن للمحفظة.
- `server.js`: تخزين بيانات العرض ومراقبة المعاملات المؤكدة.
- `index.html` و`style.css`: واجهة Meme Market.

## ملاحظة مهمة على RLO

فالإصدار الحالي، الاسم RLO داخل Meme Market كيمثل عملة الشبكة الأصلية اللي كتخرج كـ `msg.value` فالعقد. إلا بغينا RLO يكون عقد ERC-20 مستقل، خاص نسخة أخرى من Factory تستعمل `transferFrom` لعقد RLO بدل `msg.value`.

## Cancel Meme

زر `Cancel Meme` كيزيل التوكن من لائحة Rialo Market فقط. عقد ERC-20 المنشور والمعاملات القديمة مايمكنش نمسحوهم من البلوكشين. هادي خاصية مقصودة باش التاريخ والأرصدة يبقاو صحيحين.
